# Python Learning

> 从 Python 开始，最终掌握 AI 应用开发。

---

## 🎯 学习目标

- [x] Git
- [x] GitHub
- [x] Python 基础
- [ ] Python 面向对象
- [ ] Python 文件操作
- [ ] Git 熟练使用
- [ ] AI API
- [ ] RAG
- [ ] AI Agent
- [ ] MCP

---

## 🧪 运行单元测试

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements-dev.txt
pytest
```

`tests/` 下的测试通过 `script_runner` 夹具执行 `Practice/` 中的脚本，自动替换 `input()` 并捕获输出，因此不需要修改练习代码。`pytest` 默认输出 `Practice/` 的覆盖率报告。

---

## 📅 学习记录

# Python Learning
记录我的 Python 学习过程。

## Python基础阶段

Day01-Day07：Python基础语法学习

---

## 学习进度

### Day01 - 开发环境搭建

完成内容：

- Git 安装
- GitHub 创建仓库
- 第一次 Push
- VS Code 环境配置
- 完成双电脑同步环境搭建

---
### Day02 - Python基础：print与代码编辑

完成内容：

- 创建第一个 Hello World Python 文件
- 学习 Ctrl + / 快速注释
- 使用 Tab 快速编写代码
- 了解分号 `;` 可以将多行代码合并
- 练习使用 print 输出整首古诗并调整格式

遇到的问题：

- Git同步问题
- 未保存文件不会被 Git 识别

---
### Day03 - Python基础：数据与变量

完成内容：

- 学习数据的存储与运算
- 学习 Python 基本数据类型
- 理解变量的概念
- 学习变量赋值与多变量赋值

---
### Day04 - Python变量命名与赋值

完成内容：

- 学习标识符的概念
- 了解变量命名规则
- 学习Python变量命名规范
- 掌握Python变量交换方法

重点：

- 标识符只能包含字母、数字、下划线
- 变量名不能以数字开头
- 不能使用Python关键字作为变量名
- Python严格区分大小写
- Python支持直接交换变量值

---
### Day05 - Python字符串基础

完成内容：

- 学习字符串的三种定义方式
- 学习多行字符串的定义方法
- 学习字符串中的转义字符
- 学习字符串拼接方式
- 学习字符串格式化输出

重点：

- 单引号、双引号适合普通字符串定义
- 三引号可以定义多行字符串
- 字符串拼接需要注意数据类型
- 掌握 %、format、f-string 三种格式化方式

---
### Day06 - Python输入与类型转换

完成内容：

- 学习 input() 函数的使用
- 理解 input() 返回值类型
- 学习字符串类型转换
- 完成模拟银行卡取款程序

重点：

- input() 接收用户输入
- input() 默认返回字符串类型
- 不同数据类型之间需要进行转换
- 使用 int()、float() 进行类型转换

---
### Day07 - Python运算符

完成内容：

- 学习Python四类运算符
- 掌握算术运算符的使用
- 学习运算符优先级
- 学习赋值运算符
- 学习比较运算符和布尔值
- 学习逻辑运算符
- 完成平均值计算和BMI计算练习

重点：

- Python支持算术、比较、逻辑、赋值四类运算符
- 比较运算符和逻辑运算符返回布尔值(True/False)
- 使用括号可以改变运算优先级
- f-string可以格式化控制小数位数

---
### Day08 - Python条件判断if语句

完成内容：

- 学习if条件判断语句
- 理解if、else语句结构
- 学习代码缩进规则
- 使用逻辑运算符组合多个条件
- 完成登录验证案例
- 完成闰年判断案例

重点：

- if语句用于根据条件决定程序执行流程
- 冒号表示条件代码块开始
- 缩进表示代码所属范围
- 条件判断结果为True或False

---
### Day09 - Python多分支判断与match-case

完成内容：

- 学习 elif 多分支条件判断
- 学习 if 嵌套
- 学习 pass 占位语句
- 完成正数、负数、零的判断
- 完成三角形类型判断
- 学习 match-case 模式匹配
- 使用 match-case 完成工作日判断
- 使用 match-case 完成简单计算器

重点：

- elif 用于多个条件的分支判断
- if 可以进行嵌套
- pass 可以暂时占位，不执行任何操作
- match-case 适合对固定值进行多分支匹配
- if 更适合复杂条件、范围比较和逻辑组合

---
### Day10 - Python循环语句

完成内容：

- 学习 while 循环基础语法
- 学习 while-else 结构
- 使用 while 计算 1~100 的偶数和
- 学习 for 循环遍历字符串
- 学习 for-else 结构
- 理解 while 和 for 的适用场景
- 学习 range() 的使用方法
- 使用 for + range() 计算奇数累加和
- 完成 100~500 之间所有 3 的倍数求和

重点：

- while 适合根据条件重复执行，循环次数通常未知
- for 适合遍历数据或执行已知次数的循环
- range() 可以生成指定范围的整数序列
- range() 的结束值不会被包含

---
### Day12 - Python嵌套循环

完成内容：

- 学习嵌套循环的基本结构
- 使用嵌套循环打印指定长宽的长方形
- 理解外层循环与内层循环的执行关系
- 使用嵌套循环打印九九乘法表
- 练习使用 `print()` 的 `end` 参数控制输出位置

重点：

- 嵌套循环就是在一个循环内部再使用一个循环
- 外层循环控制行，内层循环控制每一行中的元素
- `print(..., end=" ")` 可以让输出保持在同一行
- 内层循环执行完成后，再执行外层循环下一次

---
### Day12 循环案例练习
- 学习了break和continue的使用
- 使用while循环实现反复登录功能
- 练习了条件判断与循环控制的结合
- 学习了random模块和随机数生成
- 使用while循环完成猜数字小游戏

---
### Day13 list基础
- 学习了Python中的容器与数据结构
- 学习了列表list的基本特点
- 学习了列表的正向索引和反向索引
- 学习了列表元素的修改和删除
- 学习了列表的遍历
- 学习了列表切片操作
- 学习了append、insert、remove、pop、sort、reverse等常用方法

---
### Day14 list相关案例 
- 练习列表的数据处理与统计
- 学习使用min、max、sum、len进行列表数据统计
- 学习列表的合并与去重
- 理解列表的解包与组包
- 学习列表推导式
- 使用列表推导式生成平方列表
- 使用列表推导式筛选偶数并计算平方

--- 
### Day15 字符串

- 学习了字符串的基本特点：不可变性、有序性、可迭代性
- 学习了字符串的索引与切片
- 学习了字符串反向切片以及字符串反转
- 学习了 `find()`、`count()`、`upper()`、`lower()` 等字符串方法
- 学习了 `split()`、`strip()`、`replace()` 等字符串处理方法
- 学习了 `startswith()` 和 `endswith()`
- 使用字符串方法完成了简单的邮箱格式判断
- 练习了回文字符串判断
- 练习了字符串反转、大小写转换和遍历

---
### Day16 元组

- 学习了元组（tuple）的基本特点
- 了解了元组与列表的区别
- 学习了元组的索引、切片和常用方法
- 学习了单元素元组的定义方式
- 学习了组包与解包
- 学习了基础解包与拓展解包
- 使用元组解包实现多个变量的值交换
- 通过学生成绩案例练习了元组、列表、循环和列表推导式的综合应用

---
### Day17 集合

- 学习了集合（set）的基本特点：无序、元素不可重复、可以修改
- 学习了空集合的定义方式 `set()`
- 学习了集合的常用方法：`add()`、`remove()`、`pop()`、`clear()`
- 学习了集合的差集、并集和交集
- 学习了使用 `&`、`|`、`-` 等运算符进行集合运算
- 学习了集合推导式
- 通过学生选课案例练习了集合的交集、差集、并集和集合推导式
- 综合使用了集合、列表、解包和 `count()` 统计学生的选课数量

---
### Day18 字典
- 学习了字典（dict）的基本概念和 key-value 键值对
- 学习了字典的添加、修改、删除和查询
- 学习了 `keys()`、`values()`、`items()` 的使用
- 学习了通过 `in` 判断字典中的 key 是否存在
- 学习了字典的遍历与解包
- 学习了嵌套字典
- 完成了基于字典的购物车管理系统
- 综合运用了 `while`、`match-case`、`if`、`continue`、`break` 等知识

---
# Day19 函数

今天开始进入函数部分，主要学习了函数的定义、调用、参数传递和返回值。

- 使用 `def` 定义函数，并理解函数需要先定义后调用
- 区分了形参和实参
- 学习了 `return` 返回结果，以及多个返回值的处理方式
- 使用解包接收函数返回的多个数据
- 了解了函数说明文档的基本写法
- 学习了函数之间的嵌套调用和 LIFO（后进先出）
- 完成了圆面积、长方形面积、三角形面积等函数练习
- 完成了字符串元音字母统计
- 完成了成绩列表最高分、最低分、平均分统计

---
### Day20 函数进阶

今天继续深入函数，重点从“怎么定义和调用函数”进入到了“函数中的变量和参数应该怎么管理”。

- 理解了全局变量与局部变量的作用范围，以及 `global` 的使用场景
- 掌握了位置参数、关键字参数和默认参数三种常见传参方式
- 学习了位置参数与关键字参数混合使用时的顺序要求
- 开始使用 `*args` 接收不定数量的位置参数
- 学习了 `**kwargs` 接收不定数量的关键字参数
- 理解了 `args` 本质是元组，`kwargs` 本质是字典
- 使用 `kwargs.get()` 实现了可选功能参数
- 完成了一个可以动态接收多组数据，并统计最大值、最小值、平均值的函数

相比 Day19，今天的重点已经从“函数的基本使用”转向了“让函数能够适应不同的调用方式”，函数的灵活性进一步提高。

---
### Day21 函数进阶：lambda与递归

今天继续学习函数的进阶使用，函数开始不再只是单独执行某段代码，而是可以作为数据传递给其他函数。

- 学习了把函数作为另一个函数的参数进行传递
- 初步接触了高阶函数的思想
- 学习了使用 `lambda` 创建简单匿名函数
- 使用 `lambda + sort(key=...)` 实现了按照字符串长度自定义排序
- 开始学习递归，通过阶乘案例理解函数自己调用自己的执行过程
- 理解了递归必须设置明确的终止条件
- 完成了超市订单金额计算案例
- 将 `*args`、默认参数、元组、列表推导式和条件判断组合到一个完整函数中
- 实现了优惠券、积分抵扣和运费等不同订单规则

Day21开始出现了“函数操作函数”和“函数调用自己”这样的用法，函数的使用方式比前两天更加灵活，也开始接近后续实际开发中常见的函数写法。